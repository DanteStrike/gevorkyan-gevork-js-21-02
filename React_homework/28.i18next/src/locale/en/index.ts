import MENU from "./menu";
import AUTH from "./auth";
import THEME from "./theme";
import REG_FORM from "./reg-form";

const EN = {
  menu: MENU,
  auth: AUTH,
  theme: THEME,
  regForm: REG_FORM,
}

export default EN;
